spring:
  datasource:
    url: jdbc:mysql://my-database.chgwsuim0apf.ap-northeast-2.rds.amazonaws.com:3306/test
    username: admin
    password: "8326272Asd!"

  config:
    import: aws-parameterstore:/camp-health-app/prod/

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

info:
  team-name: ${APP_MESSAGE}

management:
  endpoints:
    web:
      exposure:
        include: health, info
  info:
    env:
      enabled: true

logging:
  level:
    root: INFO

cloud:
  aws:
    s3:
      bucket: camp-member-hojins-files
    region:
      static: ap-northeast-2
    stack:
      auto: false # 불필요한 스택 검사를 끄기